# 为 pi 做贡献

本指南旨在节省双方时间。

## 理念

首先：**pi 的核心保持最小化**。

若你的功能不属于核心，就应做成扩展。会臃肿核心的 PR 很可能会被拒绝。

Pi 的核心存在的目的是保持精简、可扩展，从而能被扩展影响与操控。即便是扩展的 hook 点，也应充分考量与讨论，避免引入难以维护的臃肿与复杂交互。

## 唯一规则

**你必须理解自己的代码。** 若你无法解释变更做了什么、以及它们如何与系统其余部分交互，你的 PR 将被关闭。

用 AI 写代码可以。提交你自己都不理解的 AI 生成灌水代码则不行。

若你使用 agent，请从 `pi` 根目录运行，以便它自动加载 `AGENTS.md`。你的 agent 必须遵守该文件中的规则与指南。

## 贡献门槛

新贡献者的所有 issue 与 PR 默认会被自动关闭。

周五至周日提交的 issue 不保证会被审阅。若事情紧急，请在 Discord 上询问：https://discord.com/invite/3cU7Bz4UPx

维护者每日会审阅被自动关闭的 issue，并重新打开值得处理的那些。未达到下方质量标准的 issue 不会被重新打开，也不会收到回复。

批准通过维护者在 issue 上的回复完成：

- `lgtmi`：你未来的 issue 将不再被自动关闭
- `lgtm`：你未来的 issue 与 PR 将不再被自动关闭

该命令必须出现在回复开头（可选地跟在一个或多个 `@username` 提及之后）或结尾。`lgtmi` 不授予提交 PR 的权限。只有 `lgtm` 才授予提交 PR 的权限。

## Issue 质量标准

若你要开 issue，必须使用两个 GitHub issue 模板之一。

若你要开 issue，请保持简短、具体、值得阅读。

- 保持简洁。若一屏装不下，就太长了。
- 用自己的声音写（不要用 LLM 生成正文；若必须用，请用明确标注为 AI 的评论跟进）。
- 清楚说明 bug 或诉求。
- 解释为何重要。
- 若你想自己实现该变更，请说明。

若 issue 真实且写得好，维护者可能会重新打开它，或以如上所述的命令位置回复 `lgtmi` 或 `lgtm`。

## 封禁

若你两次无视本文档，或用 agent 生成的 issue 刷屏 tracker，你的 GitHub 账号将被永久封禁。

若你通过自动化大量发送 issue，你的 GitHub 账号将被永久封禁。不可反悔。

## 提交 PR 之前

除非维护者已用如上所述命令位置的 `lgtm` 批准你，否则不要开 PR。

提交 PR 之前：

```bash
npm run check
./test.sh
```

两者都必须通过。

不要编辑 `CHANGELOG.md`。Changelog 条目由维护者添加。

若你要向 `packages/ai` 添加新提供商，见 `AGENTS.md` 了解必需的测试。

## 有问题？

在 [Discord](https://discord.com/invite/nKXTsAcmbT) 上询问。

## 常见问题

### 为什么新 issue 和 PR 会被自动关闭？

pi 收到的 issue 多于维护者能实时负责任审阅的数量。许多报告未达到本指南的质量标准，或未遵循 CONTRIBUTING.md。有些是未经提交者审阅与打磨、由 agent 漫不经心地甩到仓库上的。自动关闭创造缓冲，让维护者能按自己的节奏审阅 tracker，并重新打开符合质量标准的 issue。

### 为什么周末的 issue 优先级更低？

我们在工作时间分流 tracker。这意味着周末可能积压更多 issue。周五至周日提交的内容可能被错过，或在周一审阅队列中优先级较低。若问题紧急，请在 Discord 上询问，并附上简短说明、复现步骤与相关日志。

### 为什么有些 issue 没有回复？

回复本身也是维护工作。低信息量、不清晰、重复，以及未遵循本指南的 issue，可能在无讨论的情况下被关闭。这样能把时间留给可复现的 bug、经过思考的请求，以及已把报告做得可操作的贡献者。

### 为什么不让 AI 做全部分流？

AI 可以帮助归并重复项、总结报告、发现缺失信息。它不被信任做最终维护决策。措辞精美的 AI 生成 issue 仍可能错误、误导，或调查成本高昂。人工审阅仍是最终关卡。

### 这对贡献者是否不友好？

不。这是防止倦怠与 tracker 刷屏的护栏。简短、具体、可复现的 issue 受欢迎。经过思考的贡献受欢迎。自动化灌水、理所当然的态度，以及大量低付出报告则不受欢迎。

## 在哪里了解规划？

Earendil 使用 RFC 讨论较大变更。并非全部公开，但相当一部分是公开的。可在 [rfc.earendil.com](https://rfc.earendil.com/keyword/pi/) 找到。
