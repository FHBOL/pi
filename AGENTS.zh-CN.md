# 开发规则

## 对话风格

- 回答保持简短精炼
- 提交信息、issue、PR 评论或代码中不要使用 emoji
- 不要废话或过分热情的填充语（例如写 "Thanks @user"，不要写 "Thanks so much @user!"）
- 只用技术性表述，直接了当
- 当用户提问时，先回答问题，再进行编辑或运行实现相关命令
- 回应用户反馈或分析时，先明确表示同意或不同意，再说改了什么

## 代码质量

- 在做大范围变更前、在编辑尚未完整检视过的文件前，以及被要求调查或审计时，先通读完整文件。不要依赖搜索片段做大范围变更。
- 除非绝对必要，不要使用 `any`。
- 只有一个调用点的单行辅助函数应内联。
- 到 node_modules 中查看外部 API 类型；不要猜测。
- **禁止内联 import**（`await import()`、`import("pkg").Type`、动态类型 import）。仅使用顶层 import。
- 不要为了修复过时依赖导致的类型错误而删除或降级代码；应升级该依赖。
- 在根配置检查的代码中（`packages/*/src`、`packages/*/test`、`packages/coding-agent/examples`）只使用可擦除的 TypeScript 语法（Node strip-only 模式）：不要使用参数属性、`enum`、`namespace`/`module`、`import =`、`export =`，或其他需要 JS emit 的构造。使用显式字段与构造函数赋值。
- 在移除看似有意为之的功能或代码之前，务必先询问。
- 除非用户要求，否则不要保留向后兼容。
- 永远不要硬编码按键检查（例如 `matchesKey(keyData, "ctrl+x")`）。将默认值加入 `DEFAULT_EDITOR_KEYBINDINGS` 或 `DEFAULT_APP_KEYBINDINGS`，使其可配置。
- 永远不要直接修改 `packages/ai/src/models.generated.ts`；应更新 `packages/ai/scripts/generate-models.ts`，然后重新生成。将生成的 `models.generated.ts` 差异一并提交始终可以，即使重新生成包含无关的上游模型元数据变更。

## 命令

- 代码变更后（文档除外）：运行 `npm run check`（完整输出，不要 tail）。提交前修复所有错误、警告与 info。该命令不运行测试。
- 除非用户要求，否则不要运行 `npm run build` 或 `npm test`。
- 永远不要直接运行完整 vitest 套件：它包含在存在端点/认证环境变量时会激活的 e2e 测试。所有非 e2e 测试从仓库根目录运行 `./test.sh`。否则从包根目录运行特定测试：`node ../../node_modules/vitest/dist/cli.js --run test/specific.test.ts`。
- 若你创建或修改了测试文件，运行它并迭代测试或实现直到通过。
- 对于 `packages/coding-agent/test/suite/`，使用 `test/suite/harness.ts` + faux provider。不要使用真实提供商 API、密钥或付费 token。
- 将针对特定 issue 的回归测试放在 `packages/coding-agent/test/suite/regressions/`，命名为 `<issue-number>-<short-slug>.test.ts`。
- 对于临时脚本，用 `write` 写到临时文件（例如 `/tmp`），运行、按需编辑，完成后删除。不要把多行脚本嵌进 `bash` 命令。
- 除非用户要求，否则不要提交。

## 依赖与安装安全

- 将 npm 依赖与 lockfile 变更视为需审阅的代码。直接外部依赖保持固定为精确版本。
- 更新 `undici` 时，**必须**阅读目标版本的 changelog/发布说明，并评估是否有变更可能影响功能，然后再应用更新。
- 本地灌装/更新使用 `npm install --ignore-scripts`；干净/CI 风格使用 `npm ci --ignore-scripts`。除非用户要求，否则不要运行生命周期脚本。
- 若依赖元数据变更，用 `npm install --package-lock-only --ignore-scripts` 刷新 `package-lock.json`。
- 若 `packages/coding-agent/npm-shrinkwrap.json` 需要重新生成，运行 `node scripts/generate-coding-agent-shrinkwrap.mjs`（用 `--check` 或 `npm run check` 验证）。带生命周期脚本的新依赖需要审阅，并在该脚本中有明确白名单条目；绝不要静默添加。
- 除非设置 `PI_ALLOW_LOCKFILE_CHANGE=1`，pre-commit 会阻止提交 lockfile。除非用户希望提交 lockfile 变更，否则不要绕过。

## Git

同一工作目录中可能同时运行多个 pi 会话，各自修改不同文件。会触及你自己变更之外的未暂存、已暂存或未跟踪文件的 Git 操作，会踩掉其他会话的工作。遵循这些规则：

提交时：

- 只提交你在**本会话**中更改的文件。
- 暂存明确路径（`git add <path1> <path2>`）；永远不要 `git add -A` / `git add .`。
- 提交前运行 `git status`，确认你只暂存了自己的文件。
- `packages/ai/src/models.generated.ts` 始终可与你的文件一并包含。
- 消息格式：`{feat,fix,docs}[(ai,tui,agent,coding-agent)]: <commit message>`（可选多行）。消息应信息充分且简洁。

永远不要运行（会破坏其他 agent 的工作或绕过检查）：

- `git reset --hard`、`git checkout .`、`git clean -fd`、`git stash`、`git add -A`、`git add .`、`git commit --no-verify`。

若出现 rebase 冲突：

- 只解决你修改过的文件中的冲突。
- 若冲突在你未修改的文件中，中止并向用户询问。
- 永远不要 force push。

## Issue 与 PR

贡献门槛（自动关闭工作流、`lgtm`/`lgtmi`、质量标准）见 `CONTRIBUTING.md`。

审阅 PR 时：

- 除非用户明确要求，否则不要运行 `gh pr checkout`、`git switch`，或以其他方式将工作树切换到 PR 分支。
- 使用 `gh pr view`、`gh pr diff`、`gh api`，以及针对已 fetch 的 ref 的本地 `git show`/`git diff`，在不切换分支的情况下检查 PR 元数据、提交与补丁。
- 若需要 PR 文件内容，将其 fetch/读取到临时文件，或使用 `git show <ref>:<path>`，不要切换分支。

创建 issue 时：

- 为受影响的包添加 `pkg:*` 标签（`pkg:agent`、`pkg:ai`、`pkg:coding-agent`、`pkg:tui`）；适用的全部加上。

发布 issue/PR 评论时：

- 将评论写到临时文件，并用 `gh issue/pr comment --body-file` 发布（永远不要通过 `--body` 传多行 markdown）。
- 评论保持简洁、技术性，符合用户语气。
- 每条 AI 发布的评论末尾附上发起提示所指定的 AI 生成免责声明行（例如 `This comment is AI-generated by `/wr``）。

通过提交关闭 issue 时：

- 在消息中包含 `fixes #<number>` 或 `closes #<number>`，以便合并时自动关闭 issue。多个 issue 时，对每个 issue 重复该关键字（`closes #1, closes #2`）；共享关键字（`closes #1, #2`）只会关闭第一个。

## 用 tmux 测试 pi 交互模式

在受控终端中运行 TUI（从仓库根目录）：

```bash
tmux new-session -d -s pi-test -x 80 -y 24
tmux send-keys -t pi-test "./pi-test.sh" Enter
sleep 3 && tmux capture-pane -t pi-test -p     # 启动后捕获
tmux send-keys -t pi-test "your prompt here" Enter
tmux send-keys -t pi-test Escape               # 特殊按键（也可用 C-o 表示 ctrl+o 等）
tmux kill-session -t pi-test
```

## Changelog

位置：`packages/*/CHANGELOG.md`（每个包一份）。

`## [Unreleased]` 下的小节：`### Breaking Changes`（需要迁移的 API 变更）、`### Added`、`### Changed`、`### Fixed`、`### Removed`。

规则：

- 所有新条目放在 `## [Unreleased]` 下。先通读该完整小节，再追加到已有子节；永远不要重复创建子节。
- 已发布版本小节（例如 `## [0.12.2]`）不可变；永远不要修改它们。

归属：

- 内部（来自 issue）：`Fixed foo bar ([#123](https://github.com/earendil-works/pi-mono/issues/123))`
- 外部贡献：`Added feature X ([#456](https://github.com/earendil-works/pi-mono/pull/456) by [@username](https://github.com/username))`

## 发布

**锁步版本**：所有包共享同一版本；每次发布一并更新全部包。`patch` = 修复 + 新增，`minor` = 破坏性变更。不做 major 发布。

1. **更新 CHANGELOG**：询问用户是否已对 `main` 上最新提交运行 `/cl` 提示。若没有，他们必须先运行 `/cl`，在发布前审计并更新每个包的 `[Unreleased]` 小节。

2. **本地冒烟测试**：构建未发布的 release，并在仓库外做冒烟测试（这样它无法解析 workspace 文件）：
   ```bash
   npm run release:local -- --out /tmp/pi-local-release --force
   cd /tmp

   # Node 包安装冒烟测试
   /tmp/pi-local-release/node/pi --help
   /tmp/pi-local-release/node/pi --version
   /tmp/pi-local-release/node/pi --list-models
   /tmp/pi-local-release/node/pi -p "Say exactly: ok"
   /tmp/pi-local-release/node/pi

   # Bun 二进制冒烟测试
   /tmp/pi-local-release/bun/pi --help
   /tmp/pi-local-release/bun/pi --version
   /tmp/pi-local-release/bun/pi --list-models
   /tmp/pi-local-release/bun/pi -p "Say exactly: ok"
   /tmp/pi-local-release/bun/pi
   ```
   验证 Node 与 Bun 的启动、模型/账户列表、交互式启动，以及至少一个使用预期默认提供商的真实 prompt。裸命令 `/tmp/pi-local-release/node/pi` 与 `/tmp/pi-local-release/bun/pi` 会启动交互模式；分别在 tmux 中运行，提交一个 prompt，并等待模型回复，才算交互式冒烟测试通过。失败是发布阻断项，除非用户明确接受风险。

3. **运行发布脚本**：
   ```bash
   PI_ALLOW_LOCKFILE_CHANGE=1 npm_config_min_release_age=0 npm run release:patch    # 修复 + 新增
   PI_ALLOW_LOCKFILE_CHANGE=1 npm_config_min_release_age=0 npm run release:minor    # 破坏性变更
   ```
   仅对发布命令使用 `npm_config_min_release_age=0`。否则当当前 workspace 包版本最近刚发布时，仓库正常的 npm 年龄门槛可能阻止发布时刷新 lockfile。推送前审阅发布产生的任何 lockfile 或 shrinkwrap 差异。

   发布脚本会提升所有包版本、更新 changelog、重新生成发布产物、运行 `npm run check`、提交 `Release vX.Y.Z`、打标签 `vX.Y.Z`、添加新的 `## [Unreleased]` changelog 小节、提交 `Add [Unreleased] section for next cycle`，然后推送 `main` 与标签。标签推送后不要重新运行发布脚本。

4. **CI 发布 npm 包**：推送 `vX.Y.Z` 标签会触发 `.github/workflows/build-binaries.yml`。`publish-npm` job 通过 GitHub Actions OIDC、使用环境 `npm-publish` 进行 npm trusted publishing；不需要本地 `npm publish`、`npm whoami`、OTP 或 WebAuthn 流程。

5. **若 CI 发布失败**：检查失败的 `publish-npm` job。发布辅助工具是幂等的，会跳过 npm 上已存在的包版本，因此在修复 CI 或短暂 npm 问题后，重新运行该标签工作流即可。不要对同一版本重新运行 `npm run release:patch` 或 `npm run release:minor`。

## 用户覆盖

若用户的指示与本文档中的任何规则冲突，在覆盖前先请求明确确认。只有在确认后才执行其指示。
