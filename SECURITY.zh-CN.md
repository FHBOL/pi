# 安全策略

本文档应帮助你理解 Pi 背后的安全理念，以及边界在哪里。

总体而言，Pi 是一个在本地运行的 coding agent（编程助手），处于运行它的用户的安全边界之内。监控其操作，或将其限制在容器、虚拟机或其他沙箱方案中，是用户的责任。

Pi 将本地用户账户以及该账户可写的文件，视为与 Pi 进程本身处于同一信任边界。若攻击者可以修改用户主目录、工作区、shell 启动文件、环境或 Pi 配置下的文件，他们通常就能影响 Pi 或其他本地开发工具。依赖此类既有本地写权限的报告不算安全漏洞，除非它们证明 Pi 如何授予该写权限，或跨越了操作系统权限边界。

Pi 依赖用户安装可信的扩展与 skill，并且只在可信仓库中使用 pi。因为像 `AGENTS.md` 或注释中的指令这类文件，可以轻易对 coding agent 进行 prompt injection（提示注入），而这无法被防护。

## 报告漏洞

若你认为在 pi 或本仓库的其他包中发现了安全漏洞，请通过以下任一方式私下报告：

- 发送邮件至 `security@earendil.com`，或
- 通过本仓库的 GitHub Security Advisories 开设私下报告

请包含：

- 问题描述及其影响
- 复现步骤、概念验证或相关日志
- 受影响的包、版本、提交或配置
- 任何已知缓解措施

请勿为安全敏感报告开设公开 issue。我们将审阅报告并酌情协调披露。

## 范围

已分发包、命令行工具、API 与仓库代码中的安全问题均在范围内，包括 Earendil 在 `pi.dev` 上运营的基础设施。

## 不在范围内

- 本地代码执行或沙箱行为（Pi coding agent 故意没有沙箱）
- 用户安装的 pi 扩展或 skill 的行为
- 在不受信仓库中工作的风险
- 安装不受信扩展、skill、包或工具的风险
- 由不可信 MITM 代理引起的问题
- 将 Pi 安装暴露到公网
- Prompt injection（提示注入）攻击
- 属于第三方/用户控制凭据的暴露密钥
- 需要能够在目标机器上创建、修改、删除或替换文件、目录、符号链接、环境变量、shell 配置或其他用户控制的本地状态的报告。这包括 `~/.pi`、`~/.pi/agent/models.json`、工作区文件、`AGENTS.md`、skills、扩展、扩展配置、dotfiles，以及通过 NFS、漫游配置文件或 dotfile 管理器同步的文件——除非报告表明是 Pi 本身授予了该访问权限。
- 由有意弱化的用户配置引起的问题。
- 需要针对 pi coding agent 的可信本地输入/配置才能成立的资源/DOS 主张。
- 关于恶意模型输出的报告。
- 将用户批准或用户发起的本地操作呈现为漏洞。

## 给报告者的说明

最有用的报告会展示当前、可复现的安全边界绕过，并证明影响。仅展示预期的本地 agent 行为、prompt injection，或恶意的可信扩展/skill 的报告，在本模型下不算安全漏洞。

例如，报告显示写入可信 Pi 配置文件的恶意内容会导致 Pi 执行命令、加载攻击者控制的工具、将凭据发送到攻击者控制的端点，或以其他方式改变行为——这类报告不在范围内。

可能时，请包含确切的受影响路径、包版本或提交 SHA、配置，以及针对最新 release 或最新 `main` 的概念验证。对于依赖相关报告，请提供已随产品分发的依赖受影响、且问题可通过 Pi 触达的证据。对于暴露密钥报告，请提供该凭据归 Earendil 所有，或可访问 Earendil 运营的基础设施或服务的证据。
